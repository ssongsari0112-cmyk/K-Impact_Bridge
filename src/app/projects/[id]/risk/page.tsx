import { PlaceholderSection } from "@/components/workspace/PlaceholderSection";

export default function RiskTabPage() {
  return (
    <PlaceholderSection
      title="Risk"
      agent="Strategy Agent"
      description="주요 리스크와 대응방안이 이곳에 표시됩니다."
    />
  );
}
